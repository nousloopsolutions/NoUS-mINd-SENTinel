# MIND Sentinel — .gitattributes
# Enforces consistent line endings across Windows, Linux, Termux
# Prevents CRLF corruption that can break scripts on Android

# Default: normalize to LF on commit
* text=auto eol=lf

# Python — always LF
*.py    text eol=lf
*.pyw   text eol=lf

# Shell scripts — always LF (critical for Termux compatibility)
*.sh    text eol=lf
*.bash  text eol=lf

# Config files — LF
*.yml   text eol=lf
*.yaml  text eol=lf
*.json  text eol=lf
*.toml  text eol=lf
*.cfg   text eol=lf
*.ini   text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
*.env   text eol=lf

# Git hooks — LF (must be LF or they break on Unix)
.git/hooks/* text eol=lf

# Binary — no line ending conversion
*.db     binary
*.sqlite binary
*.sqlite3 binary
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.zip    binary
*.gz     binary
*.tar    binary
*.whl    binary
*.pyc    binary
*.pyo    binary
*.so     binary
*.dll    binary
*.exe    binary
*.pdf    binary
