# MIND Sentinel — Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env — it is in .gitignore
#
# Usage:
#   cp .env.example .env
#   (edit .env with your actual values)

# Ollama host (default is fine for local install)
OLLAMA_HOST=http://localhost:11434

# Default model (change to match what you have pulled)
OLLAMA_MODEL=llama3:8b-instruct

# Optional: Google Cloud (only if using Sheets export — not required)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# TARGET_SPREADSHEET_ID=your-sheet-id-here

# Optional: Gemini API (only if using cloud fallback — not required for offline mode)
# GEMINI_API_KEY=your-key-here
